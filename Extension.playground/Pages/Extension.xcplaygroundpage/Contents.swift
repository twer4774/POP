import UIKit

/*
 값타입의 개체를 확장한다.
 단점:
    - 서브클래싱을 할 수 없다.
    - extension은 기능을 확장하는 개념이므로, override가 불가하다.
    - 저장프로퍼티를 정의 할 수 없다. 연산프로퍼티인 경우 정의 가능하다.
    - @objc를 이용할 수 없다. => Selector를 지정할 수 없다.
 */
